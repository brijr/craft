import { Layout, Box } from "@/components/craft";

# Layout Component

The Layout component is the foundation of your application's structure in Craft Design System. It sets up the basic HTML structure and applies global styles.

## Import
